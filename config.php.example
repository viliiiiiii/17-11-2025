<?php
// Copy this file to config.php and fill in production credentials.

define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'abrm');
define('DB_USER', 'abrm');
define('DB_PASS', 'secret');
define('DB_CHARSET', 'utf8mb4');

define('BASE_URL', 'http://localhost'); // no trailing slash

// S3-compatible storage credentials
// Example for MinIO running locally: http://127.0.0.1:9000

define('S3_ENDPOINT', 'https://your-s3-endpoint');
define('S3_KEY', 'your-access-key');
define('S3_SECRET', 'your-secret-key');
define('S3_BUCKET', 'abrm');
define('S3_REGION', 'us-east-1');
define('S3_USE_PATH_STYLE', true); // set false if using virtual-hosted-style endpoints

// Optional: override the URL base used to serve files.
define('S3_URL_BASE', ''); // leave blank to derive from endpoint + bucket.

// Security

define('SESSION_NAME', 'abrm_session');
define('CSRF_TOKEN_NAME', 'csrf_token');

define('APP_TIMEZONE', 'UTC');

define('APP_TITLE', 'ABRM Management');

// Web Push / VAPID configuration (fill after generating keys via scripts/generate_vapid.php)
define('WEB_PUSH_VAPID_PUBLIC_KEY', getenv('WEB_PUSH_VAPID_PUBLIC_KEY') ?: '');
define('WEB_PUSH_VAPID_PRIVATE_KEY', getenv('WEB_PUSH_VAPID_PRIVATE_KEY') ?: '');
define('WEB_PUSH_VAPID_SUBJECT', getenv('WEB_PUSH_VAPID_SUBJECT') ?: 'mailto:admin@example.com');

define('NOTIFICATIONS_SERVICE_URL', getenv('NOTIFICATIONS_SERVICE_URL') ?: 'http://127.0.0.1:8001');
define('NOTIFICATIONS_VAPID_PUBLIC_KEY', getenv('NOTIFICATIONS_VAPID_PUBLIC_KEY') ?: WEB_PUSH_VAPID_PUBLIC_KEY);
